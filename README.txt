Le dossier UtilisationService représente une solution qui contient un projet qui montre comment est utilisé un service web SOAP que l'on appelle (Calcette)

Le dossier ServiceHotels correspond à une solution qui contient deux projets correspondent aux exercices 4:

-Un projet qui crée un service web SOAP qui represent plusieurs hotels.

-Un projet Agence qui représente un client qui accède au service web SOAP de l'hotel en appelant ses deux méthodes principales.

Une interface graphique a été faite pour l'agence (Client), mais pas de base de données, pour la question 3, l'image est à dans le dossier img du projet ServiceHotel,

L'agence l'affiche dans une interface (WindowsForm) après réservation.

Membre du groupe :
Ramzi zeroual 
milissa dadi 